
plugin.tx_formrelay_json_request {
  settings {
    enabled = {$plugin.tx_formrelay_json_request.settings.enabled}
    url = {$plugin.tx_formrelay_json_request.settings.url}

    cookies =

    gate {
    }

    defaults {
      # request-field -> request-value
      # some_id = xyz
    }

    fields {
      mapping {
        # form-field -> request-field
        # first_name = first-name
      }

      unmapped.ignore = 1
    }
    values {
      ignoreIfEmpty = 1
    }
  }
}
